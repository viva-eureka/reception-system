# 要件定義

> ステータス: 仮版 v7 / 最終更新: 2026-06-10  
> 変更: 招待フロー刷新（Googleカレンダー Apps Script 自動連携）・管理画面招待タブをシンプル化  
> 参照: [ユーザーシナリオ一覧](./user-scenarios.md)

---

## 1. プロジェクト概要

### 背景

現在、受付管理に「ラクネコ」（月額課金のiPadネイティブアプリ）を使用している。
月額コスト削減と、招待・カレンダー登録・当日受付フローの煩雑さを解消するために自社開発へ切り替える。

### 目的

| # | 目的 |
|---|---|
| 1 | ラクネコの月額費用を削減する |
| 2 | 招待作成〜来訪当日までのフローを自社業務に最適化する |
| 3 | Google Workspace（Calendar・Chat）との連携を自動化する |
| 4 | ISO27001に準拠したセキュリティ要件を満たす |
| 5 | 将来的な機能拡張の自由度を確保する |

### スコープ（フェーズ1）

- 受付端末（iPadキオスク） — PWA（Webアプリ）
- 管理コンソール（PC/iPad ブラウザ）
- Google Chat 通知
- 入退館記録

---

## 2. 機能要件

### 2-1. 受付端末（キオスク画面）

| ID | 機能 | 詳細 | 優先度 |
|---|---|---|---|
| F-01 | 待ち受け画面 | ロゴ・時計・日付・あいさつ文を表示。タップで受付開始 | Must |
| F-02 | 営業時間外表示 | 設定時間外は「ただいま営業時間外です」を表示 | Must |
| F-03 | **時間外受付の許可設定** | 設定でONにすると時間外でも受付操作を許可する（画面には時間外である旨を表示したまま） | Must |
| F-04 | **招待済みウェルカム表示** | 予約15分前〜開始時刻に「お打ち合わせ予約 ○○様」ボタンを待ち受けに表示 | Should |
| F-05 | 受付メニュー | 「配送業者」「ご予約・招待の方」の選択 | Must |
| F-06 | QR受付 | カメラでQRコードを読み取り、招待情報と照合して受付完了 | Must |
| F-07 | 名刺撮影受付 | カメラで名刺を撮影。**OCRは実装しない**（iPadインカメラでは精度不足のため）。撮影した画像をGoogle Chatで担当者に自動送信し、人間が目視確認。来訪者はキーボードで氏名を入力して受付完了 | Should |
| F-08 | キーボード受付 | 氏名・会社名をテキスト入力して受付 | Must |
| F-09 | **音声入力受付** | 端末マイクで氏名を音声入力（Web Speech API、iOS Safari対応済み）。**実装確定**。端末マイク権限設定が必要 | Should |
| F-10 | 配送業者ワンボタン | ボタン1回で配送受付完了・通知送信 | Must |
| F-11 | 受付完了画面 | あいさつ文を表示後、一定秒数で待ち受けに自動復帰 | Must |
| F-12 | 背景テーマ | 5種類のテーマを設定画面から選択可能（端末画面には非表示） | Nice |

### 2-2. 招待管理

| ID | 機能 | 詳細 | 優先度 |
|---|---|---|---|
| F-13 | **招待の自動作成（メインフロー）** | Googleカレンダーの予定に社外ゲストを追加すると Apps Script が `/api/invitation` に POST して招待状を自動発行。担当者の Google Chat に招待 URL が届く。`gas/reception-calendar.gs` に実装済み | Must |
| F-13b | 管理画面からの手動作成（緊急用） | 氏名・担当者・日時の3フィールドで作成。カレンダー不使用・例外対応用のみ。作成後にインラインで URL を表示。実装済み | Should |
| F-14 | QRコード生成 | 招待ごとに一意のQRコードを生成。実装済み | Must |
| F-15 | 招待 URL の共有 | 担当者は Chat に届いた URL を LINE 等で来訪者に転送。管理画面でも一覧の🔗ボタンから URL をコピー可能。実装済み | Must |
| F-16 | **カレンダー連携 Apps Script** | `reception-calendar.gs` を Google Apps Script に設置し `setupTrigger()` を一度実行するだけで有効化。サービスアカウント・ドメイン委任は不要。実装済み | Must |
| F-17 | 招待一覧 | 作成済み招待の一覧表示（降順）・キャンセル・ステータス管理。実装済み | Must |
| F-18 | **ノーショー管理** | 予約開始時刻から設定した時間（例:15分）が経過しても受付未完了の場合、担当者にGoogle Chat自動通知「○○様が来社されていますか？」。担当者が「来ていない」と応答またはタイムアウトした場合、招待をノーショーとしてマークし会議室予約を自動開放する | Must |

### 2-3. 入退館管理

| ID | 機能 | 詳細 | 優先度 |
|---|---|---|---|
| F-19 | 入館記録（自動） | 受付完了時に自動で入館記録を作成（氏名・会社・種別・担当・日時） | Must |
| F-20 | 退館記録（手動） | 管理画面から任意のタイミングで退館記録を追加 | Must |
| F-21 | **自動退館記録** | 設定した時刻（例: 23:59）を過ぎた在館者に自動で退館記録を作成。**実装確定**。将来拡張として会議終了時間と照合して担当者に「退館されましたか？」確認通知を送る機能も検討 | Must |
| F-22 | 記録一覧 | 時系列での入退館ログ表示、在館状態の判定 | Must |
| F-23 | **CSVエクスポート** | 表示中の列設定でCSV出力（BOM付きUTF-8でExcel対応）。モック実装済み | Must |
| F-24a | **表示項目カスタマイズ** | 記録一覧で表示する列（氏名/会社/区分/動作/担当/人数/メモ）をON/OFFで切替。モック実装済み | Should |

### 2-4. 通知（Google Chat App）

> **方式変更**: Incoming Webhook（送信のみ）→ **Google Chat App**（送受信・インタラクティブカード対応）  
> 業務効率化の核心部分のため**フェーズ1で実装**する。

| ID | 機能 | 詳細 | 優先度 |
|---|---|---|---|
| F-24 | **来訪通知（インタラクティブカード）** | 受付完了時に共有スペースへカード通知。カードには「✋ 対応する」ボタンを表示。ボタンを押した人の名前・時刻を記録し、カードを「○○が対応中」に自動更新 | Must |
| F-25 | **担当者DM通知** | 来訪者に紐づく担当者のGoogle Chat DMへ直接通知。「🚶 今向かいます」「⏰ 少々お待ちを」ボタンを含む。担当者への中継連絡（現行の手動ステップ）を自動化 | Must |
| F-26 | 配送通知 | 配送受付時に共有スペースへカード通知（対応ボタン付き） | Must |
| F-27 | 退館通知 | 退館記録時に共有スペースへ通知 | Should |
| F-28 | **時間外受付通知** | 時間外受付時、通知カードに「時間外受付」を明記 | Should |
| F-29 | **会議終了リマインダー** | 予約終了時刻に担当者＋受付対応者へ「片付けリマインダー」通知 | Should |

### 2-5. 設定・管理

| ID | 機能 | 詳細 | 優先度 |
|---|---|---|---|
| F-28 | 受付時間設定（曜日別） | 月〜日ごとに開始・終了時刻と有効/定休日を個別設定。モック実装済み | Must |
| F-29 | **時間外受付許可** | 時間外でも受付操作を許可するON/OFF設定。モック実装済み | Must |
| F-30 | 会社情報設定 | 会社名・タグラインを変更 | Must |
| F-31 | **受付ボタン管理** | iPad受付メニューのボタンを追加・削除・並び替え。名前・絵文字・カラー・種別（配送/来訪/カスタム）を設定。モック実装済み | Must |
| F-32 | 会議室管理 | 会議室名の追加・削除 | Must |
| F-33 | **Chat App設定** | Google Chat App の認証情報（Service Account / App credentials）を設定。通知先スペースID・担当者UserIDマッピングを管理 | Must |
| F-34 | プライバシー設定 | 受付完了画面の表示モード（氏名のみ／会社名+氏名／汎用文）を選択 | Must |
| F-35 | テーマ設定 | 待ち受け背景テーマを選択（5種類。設定画面のみ、端末に切替UIは表示しない）。モック実装済み | Nice |
| F-36 | 管理者認証 | 管理コンソールへのログイン制限（Supabase Auth） | Must |

### 2-6. 会議室ダッシュボード

| ID | 機能 | 詳細 | 優先度 |
|---|---|---|---|
| F-37 | **現在の空き状況** | 管理コンソールの「会議室」タブで全会議室の現在の空き/使用中を一覧表示。社内会議の調整にも利用可能。モック実装済み | Must |
| F-38 | **終日タイムライン** | 当日の会議室ごとの予約状況をタイムライン形式で表示。現在時刻を縦線で示す。本番はGoogle Calendar freebusy API（個人カレンダーは参照しない）。モック実装済み | Must |
| F-39 | **日付切替** | タイムラインを翌日・翌々日など別日付で確認できる | Should |

---

## 3. 非機能要件

### 3-1. ユーザビリティ

| # | 要件 |
|---|---|
| U-01 | 来訪者が初見で迷わず操作できること（ガイドテキスト・大きなボタン） |
| U-02 | 受付完了まで3タップ以内を目標 |
| U-03 | iPadのソフトキーボード表示時にレイアウトが崩れないこと |
| U-04 | 管理コンソールはPC（1280px以上）とiPad（768px以上）の両方で使用できること |
| U-05 | 体温測定機能は不要（コロナ禍対応機能は搭載しない） |

### 3-2. パフォーマンス

| # | 要件 |
|---|---|
| P-01 | 受付完了まで体感3秒以内（ネットワーク遅延を除く） |
| P-02 | 初回ロード（iPad Safari）で5秒以内に操作可能になること |

### 3-3. 可用性・信頼性

| # | 要件 |
|---|---|
| V-01 | 受付時間内の稼働率99%以上を目指す |
| V-02 | Wi-Fi一時切断時でも待ち受け画面の表示は維持されること（Service Worker / キャッシュ） |
| V-03 | Google Chat通知失敗時はリトライまたはエラーログを残すこと |
| **V-04** | **Supabaseの自動バックアップを有効化し、復旧手順を文書化すること（ISO27001 可用性要件）** |

### 3-4. セキュリティ（ISO27001 対応）

| # | 要件 | ISO27001対応 |
|---|---|---|
| S-01 | 管理コンソールは認証必須（Supabase Auth）。受付端末は認証不要 | 認証強化 ✅ |
| S-02 | QRコードのトークンは予測不可能な一意の値（UUID等）を使用 | 個人情報保護 ✅ |
| S-03 | 受付完了画面への会社名表示はデフォルト無効 | 個人情報最小化 ✅ |
| S-04 | 来訪者の個人情報はSupabaseのRow Level Security（RLS）で保護 | 権限管理 ✅ |
| S-05 | 入退館記録の保持期間は**3年**（ISO27001基準がある場合はそれに従う）。期限到来後は自動削除または匿名化 | 保存期間 ✅ |
| **S-06** | **通信はHTTPS、Supabaseの保存データは暗号化（at rest）を利用** | データ暗号化 |
| **S-07** | 受付端末は**アクセスガイド（Guided Access）で固定**（1台運用のため確定）。複数拠点・複数テナントに拡張する際はMDM（Jamf等）に切り替える | 端末保護 |
| **S-08** | **管理コンソールの操作（登録・変更・削除・閲覧）をSupabaseのログで追跡可能にする** | 操作ログ |
| **S-09** | **IPアドレス制限を管理コンソールに設けることを検討（社内IPのみアクセス可）** | アクセス制限 |

### 3-5. 監査対応

| # | 要件 |
|---|---|
| **A-01** | **入退館記録をCSVエクスポートできること（期間・担当者・種別でフィルタ可）** |
| **A-02** | 管理画面の操作ログを**10年間**保持し、**スーパー管理者のみ**閲覧・エクスポートできること |
| **A-03** | 入退館記録の保持期間設定（デフォルト3年）と、期限到来後の自動削除機能を備えること |

### 3-6. 保守性

| # | 要件 |
|---|---|
| M-01 | フロントエンドとバックエンドを分離し、片方の変更が他方に影響しないこと |
| M-02 | GitHubで管理し、mainマージで自動デプロイ（Vercel）されること |
| M-03 | 設定（受付時間・テーマ等）はUIから変更できること（コード変更不要） |

---

## 4. システム構成（案）

```
┌─────────────────────────────────────────────┐
│                  クライアント                     │
│  受付端末 (iPad)          管理コンソール (PC)    │
│  PWA / Safari（Guided Access）  ブラウザ        │
└──────────────┬──────────────────┬────────────┘
               ▼                  ▼
┌─────────────────────────────────────────────┐
│              Next.js (Vercel)                 │
│  受付画面 / 管理画面 / API Routes             │
└──────────────┬──────────────────┬────────────┘
               ▼                  ▼
┌─────────────┐          ┌──────────────────────┐
│  Supabase   │          │  外部サービス           │
│  - Postgres │          │  - Google Chat         │
│  - Auth     │          │  - Google Calendar     │
│  - Storage  │          │  - OCR (Vision API)    │
│  - Realtime │          │  - メール送信 (Resend) │
│  - 操作ログ │          └──────────────────────┘
└─────────────┘
```

### 採用技術（案）

| 役割 | 技術 | 理由 |
|---|---|---|
| フロントエンド | Next.js + React + Tailwind CSS | 管理画面と受付端末を同一コードベースで管理 |
| バックエンド / DB | Supabase | Auth・RLS・Realtime・自動バックアップが揃っている |
| ホスティング | Vercel | GitHub連携で自動デプロイ。無料枠あり |
| メール送信 | Resend | 月3000通まで無料。QR添付対応 |
| カレンダー連携（フェーズ1） | Google Calendar ブラウザ拡張機能 | 管理者権限不要・軽量。拡張機能をインストールするだけで運用開始可能 |
| カレンダー連携（フェーズ2） | Google Calendar API（サービスアカウント） | 招待作成と連動した完全自動化。会議室リソース予約にドメイン全体委任が必要 |
| ~~名刺OCR~~ | ~~Google Cloud Vision API~~ | ~~OCR実装は見送り。名刺画像をChatに送信して人間が確認する方式に変更~~ |
| 通知 | **Google Chat App**（フェーズ1） | Incoming Webhookから切り替え。インタラクティブカード・ボタン押下時コールバック・DM送信に対応 |

---

## 5. 開発フェーズ

### フェーズ1（優先実装）― ラクネコ代替＋業務効率化

| # | タスク | 備考 |
|---|---|---|
| 1 | Supabase テーブル設計（invitations / logs / settings / audit_logs / responders） | respondersテーブルに対応者・時刻を記録 |
| 2 | 招待管理のCRUD（Supabase連携） | |
| 3 | 入退館記録の永続化・CSVエクスポート | |
| 4 | **Google Chat App 実装**（インタラクティブカード） | 共有スペース通知＋「対応する」ボタン＋対応者記録 |
| 5 | **担当者DM通知**（来訪者→担当者へ直接通知） | 「今向かいます」「少々お待ちを」ボタン付き |
| 6 | 管理コンソールの認証（Supabase Auth） | |
| 7 | 受付時間の曜日別設定 | モック実装済み |
| 8 | 自動退館記録（設定した時刻で自動実行） | |
| 9 | Vercel へのデプロイ・自動デプロイ設定 | |
| 10 | 操作ログの記録（Supabase） | |

### フェーズ2（次期実装）

| # | タスク |
|---|---|
| 1 | Google Calendar 連携（招待作成→予定・会議室自動登録。サービスアカウント設定済み） |
| 2 | 招待メール実送信（Resend + QR添付） |
| 3 | 招待済みウェルカム表示（15分前から待ち受けにボタン表示） |
| 4 | ノーショー自動通知（未受付招待→担当者へChat確認→会議室開放） |
| 5 | 音声入力受付（Web Speech API・モック実装済み） |
| 6 | 会議終了リマインダー（予約終了時刻に片付け通知） |
| 6 | PWA対応（Service Worker / オフラインキャッシュ） |
| 7 | データ保持期間設定・自動削除（入退館3年 / 操作ログ10年） |
| 8 | スーパー管理者権限・操作ログ閲覧機能 |

### フェーズ3（将来検討）

- 退館QRスキャン
- 会議終了時間照合による退館確認通知
- 来訪分析・レポート
- 複数拠点対応（MDM連携）
- IPアクセス制限

---

## 6. 決定済み事項

すべての未決定事項が確定しました。

| # | 項目 | 決定内容 |
|---|---|---|
| ~~TBD-01~~ | 名刺OCR | **見送り**。iPadインカメラでは精度不足のためOCR実装なし。名刺画像をGoogle Chatに自動送信し担当者が目視確認する方式に変更 |
| ~~TBD-02~~ | iPadキオスク運用 | **アクセスガイド（Guided Access）で確定**。複数拠点・複数テナントに拡張する際はその時点でMDMを検討 |
| ~~TBD-03~~ | データ保持期間 | **入退館記録は3年**（ISO27001基準がある場合はそれを優先）。期限到来後は自動削除または匿名化 |
| ~~TBD-04~~ | カレンダー連携の権限 | **実施可能と確認**。サービスアカウント＋ドメイン全体委任はGoogle Workspace管理者で対応可。フェーズ2で実装 |
| ~~TBD-05~~ | 退館フロー | **自動退館設定を実装**（Must）。将来拡張として会議終了時間照合による担当者への退館確認通知も検討 |
| ~~TBD-06~~ | ノーショー通知 | **自動通知を実装**（Must）。予約開始から設定時間後に担当者へChat確認通知。未来訪確定でノーショーマーク＋会議室自動開放 |
| ~~TBD-07~~ | 音声入力 | **実装確定**（フェーズ2）。Web Speech APIはiOS Safari対応済み。端末マイク権限設定が必要 |
| ~~TBD-08~~ | 操作ログ保持期間・閲覧権限 | **10年保持、スーパー管理者のみ閲覧・エクスポート可能** |
